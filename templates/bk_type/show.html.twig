{% extends 'base.html.twig' %}

{% block title %}Type{% endblock %}

{% block body %}
    <div class="container">
        <h1>Type</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ type.id }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('b_k_type_index') }}">back to list</a>

        <a href="{{ path('b_k_type_edit', {'id': type.id}) }}">edit</a>

        {{ include('bk_type/_delete_form.html.twig') }}
    </div>
{% endblock %}
