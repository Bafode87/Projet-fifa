{% extends 'base.html.twig' %}

{% block title %}Edit Type{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit Type</h1>

        {{ include('bk_type/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('b_k_type_index') }}">back to list</a>

        {{ include('bk_type/_delete_form.html.twig') }}
    </div>
{% endblock %}
