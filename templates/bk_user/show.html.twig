{% extends 'base.html.twig' %}

{% block title %}Utilisateur {{ user.email }}{% endblock %}

{% block body %}
    <div class="container">
    <h1>Utilisateur {{ user.email }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Adresse e-mail</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('b_k_user_index') }}" class="btn btn-primary">Retour à la liste</a>

    <a href="{{ path('b_k_user_edit', {'id': user.id}) }} " class="btn btn-primary">Modifier</a>

    {{ include('bk_user/_delete_form.html.twig') }}
        <div>
{% endblock %}
