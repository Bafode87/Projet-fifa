{% extends 'base.html.twig' %}

{% block title %}Tag{% endblock %}

{% block body %}
    <div class="container">
        <h1>Tag</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ tag.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ tag.nom }}</td>
                </tr>
                <tr>
                    <th>Couleur</th>
                    <td>{{ tag.couleur }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('b_k_tag_index') }}">back to list</a>

        <a href="{{ path('b_k_tag_edit', {'id': tag.id}) }}">edit</a>

        {{ include('bk_tag/_delete_form.html.twig') }}
    </div>
{% endblock %}
