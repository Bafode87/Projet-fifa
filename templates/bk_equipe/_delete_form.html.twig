<form method="post" action="{{ path('b_k_equipe_delete', {'id': b_k_equipe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ b_k_equipe.id) }}">
    <button class="btn btn-primary" style="margin-top: 1%">Supprimer</button>
</form>
