{% extends 'base.html.twig' %}

{% block title %}Video{% endblock %}

{% block body %}
    <div class="container">
        <h1>Video</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ video.id }}</td>
                </tr>
                <tr>
                    <th>Titre</th>
                    <td>{{ video.Titre }}</td>
                </tr>
                <tr>
                    <th>Lien</th>
                    <td>{{ video.lien }}</td>
                </tr>
                <tr>
                    <th>Postedat</th>
                    <td>{{ video.postedat ? video.postedat|date('Y-m-d') : '' }}</td>
                </tr>
                <tr>
                    <th>Author</th>
                    <td>{{ video.author }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ video.description }}</td>
                </tr>
            <tr>
                <th>Tags</th>
                <td>  {{ video.tags|join(', ') }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('b_k_video_index') }}" class="btn btn-primary">Retour à la liste</a>

        <a href="{{ path('b_k_video_edit', {'id': video.id}) }}" class="btn btn-primary">Modifier</a>

        {{ include('bk_video/_delete_form.html.twig') }}
    </div>
{% endblock %}
